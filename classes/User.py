def validate_user(email, password):
    pass

'''
    class User(object):
        def __init__(self, name, email, password, address):
            self.name = name
            self.email = email
            self.password = password
            self.address = address
    
        def register(self):
            print(self.name, self.email, self.password, self.address)
            pass
    
    
    class SellerShelter(User):
        def __init__(self, name, email, password, address, category, cnpj, docs, description):
            User.__init__(self, name, email, password, address)
            self.category = category
            self.cnpj = cnpj
            self.docs = docs
            self.description = description
            self.pets = []
            self.photos = []
    
        def register(self):
            print(self.name, self.email, self.password, self.address)
            pass
    
        def add_pet(self):
            pass
'''
